<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login User</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            background-image: url('./bck.jpg'); /* Make sure this path is correct */
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-color: #b6dbda; /* Fallback color in case image doesn't load */
        }

        .container {
            border: 1px solid #ccc;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 500px;
            background-color: rgb(15, 104, 74);
            text-align: center;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center; /* Center-aligns all form elements including the button */
        }

        label, input {
            margin-bottom: 10px;
            text-align: left;
            width: 100%;
        }

        input[type="text"] {
            padding: 8px;
            font-size: 14px;
            border: 1px solid #ccc;
            border-radius: 4px;
            width: 100%;
            box-sizing: border-box;
        }

        button {
            width: 200px;
            padding: 10px;
            background-color: rgb(26, 54, 104); /* Button background color */
            color: white; /* Button text color */
            font-size: 16px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:hover {
            background-color: darkred; /* Darker shade on hover */
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Login</h1>
        <form action="/user/login" method="post">
            <!-- <label>Full Name</label>
            <input type="text" required name="name" > -->
            <label>E-mail</label>
            <input type="text" required name="email" placeholder="example@abj.com">
            <label>Password</label>
            <input type="text" required name="password">

            <button type="submit">LogIn</button>
        </form>
    </div>
</body>
</html>
